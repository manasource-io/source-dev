# Chia Seeds — Research Notes

- **Resource**: `resources/nutrition/food/chia-seeds.md`
- **Leads**: Routine source-check pass — no new Reddit posts processed this session.

## Claims Reviewed

- Harvard Nutrition Source URL permanently redirected: `www.hsph.harvard.edu/nutritionsource/food-features/chia-seeds/` → `nutritionsource.hsph.harvard.edu/food-features/chia-seeds/`. Confirmed new URL is valid and content is substantive. The page accurately represents chia's benefits as primarily inferred from nutrient composition (fiber, ALA omega-3) rather than from specific chia-seed RCTs, which aligns with the resource's `credibility: moderate` rating.

## Changes Made

- Updated reference URL in resource frontmatter from old `hsph.harvard.edu` domain to canonical `nutritionsource.hsph.harvard.edu` domain.
- Updated `source-checks.csv`: marked old URL as `removed`, added new URL as `valid`.
- `lastResearched` not changed (URL fix only, no content changes warranted).

## Notes

- The body remains below 1300 characters and lacks `description`, `benefits`, `benefitLevel`, and `overallScore` attributes. These should be added in a future session.
- The chia-seeds resource is substantively accurate but underdeveloped compared to the resource spec format.
