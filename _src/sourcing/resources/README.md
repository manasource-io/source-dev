# Resource sourcing records

This directory will hold per-resource sourcing history organized by canonical resource ID.

Example shape:

```text
resources/
  supplement/
    creatine/
      source-checks.csv
      2026-04-09.md
```

The folder layout here should follow the canonical rules in `_src/workflow/taxonomy-map.md`, not legacy `poc/research` paths.
