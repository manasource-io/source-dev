# Lead config

This directory is reserved for machine-readable lead-ingestion configuration.

Planned files include:

- `communities.json`
- `queries.json`
- `pruning-rules.json`

Pass A only establishes the structure and policy. Pass B should populate the actual config files.
